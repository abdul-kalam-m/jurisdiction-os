// §6.3: this project's data is small enough to ship directly with the
// static site (bundled from web/public/data/), unlike the owner's other,
// parcel-scale projects which need a separate R2 origin for large tiles.
export const DATA_BASE_URL = '/data'

export const DISCLAIMER =
  'Demonstration product. Jurisdiction Intelligence OS is a portfolio project built entirely ' +
  'from public records. Benchmarks are estimates from published permit data; requirements ' +
  'summaries are curated from official sources and may be outdated. Nothing here is legal ' +
  'advice or a substitute for confirming requirements with the jurisdiction.'
